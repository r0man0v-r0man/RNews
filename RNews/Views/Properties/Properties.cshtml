@model RNews.Models.ViewModels.PropertyViewModel

@{
    ViewData["Title"] = "Properties";
}
   
<div class="row justify-content-center ">
    <div class="col-md-8 row ">
        <div class="col-md-4 box-shadow">
            <img src="@Model.ImagePath" alt="User Avatar" class="img-fluid" id="user-avatar" />
            
            <partial name="UploadAvatar" />
        </div>
        <div class="col-md-8">
            <h4 class="text-center font-weight-light">@foreach (var role in Model.Roles)
            {
                <strong> @role</strong> 
            }
            </h4>
            <hr />
           
                @Html.HiddenFor(m => m.PropertyViewModelId)
                <div class="form-group row">
                    <label for="user-property-name" class="col-sm-2 col-form-label font-weight-bold">Name</label>
                    <div class="col-sm-10">
                        <input type="text" class="form-control-plaintext" id="user-property-name" value="@Model.Name">
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-sm-2 font-weight-bold">E-Mail</div>
                    <div class="col-sm-10">
                        <div id="user-property-email">@Model.Email</div>
                    </div>
                </div>
                <div class="form-group row">
                    <label for="user-property-description" class="col-sm-12 col-form-label font-weight-bold">Description:</label>
                    <div class="col-sm-12">
                        <input type="text" class="form-control-plaintext last" id="user-property-description" value="@Model.Description">
                    </div>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary btn-block" id="user-property-btn">Ok</button>
                </div>
           
        </div>
        
    </div>
</div>

@section Scripts {
    <script src="~/lib/signalr/dist/browser/signalr.js"></script>
    <script src="~/js/UserProperty.js"></script>
}