@{
    ViewData["Title"] = "Home Page";
}

<div class="row">
    @foreach (var item in ViewBag.LastAdded)
    {

        <div class="col-md-4">
            <div class="card mb-4 box-shadow">
                <div class="card-body">
                    <h5 class="card-title">@item.Title</h5>
                    <p class="card-text">@item.Description</p>
                    <div class="d-flex justify-content-between align-items-center">
                        @Html.ActionLink("Читать", "show", "publication", new { id = item.PostId }, new { @class = "btn btn-link" })
                        <small class="text-muted">@item.Category | @item.Created.ToString("d")</small>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
<div class="row">
    @foreach (var item in ViewBag.TopRatingPost)
    {
        <div class="col-md-4">
            <div class="card mb-4 box-shadow">
                <div class="card-body">
                    <h5 class="card-title">@item.Title</h5>
                    <p class="card-text">@item.Description</p>
                    <div class="d-flex justify-content-between align-items-center">
                        @Html.ActionLink("Читать", "show", "publication", new { id = item.PostId }, new { @class = "btn btn-link" })
                        <small class="text-muted">@item.Category | @item.Created.ToString("d")</small>
                    </div>
                </div>
            </div>
        </div>
    }
</div>